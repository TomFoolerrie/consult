# Brief — {{agent}}

Author name: `{{agent}}`. Taxonomy: {{taxonomy}}. Context-profile: `{{context}}`.
Theme axes required on every row: {{theme_axes}}.

Append: `{{sh}} ledger append themes --author {{agent}} --from <rows.jsonl>`

Example row:
```json
{{theme_example}}
```
