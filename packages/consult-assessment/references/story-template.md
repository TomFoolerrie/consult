# [Client] — Finance Assessment

## Where you are
What the business is, what the finance function has built, what is working. `[fig-strengths]`, strength findings by id. One paragraph on why the gaps exist (growth, transitions, tooling — not blame).

## What we found
Themes as cause → consequence. One short section per theme: the pattern, the root cause, the impact, the two or three findings that show it best. Open with the figure that meets the client's own framing (see story note), then the house heat maps and `[fig-scorecard]` as they earn their place.

## What it means for the audit / the business
Consequences if nothing changes, in the client's terms. Severity-3 findings by id.

## What to do
Recommendations as principles (short), then initiatives as a sequence: `[fig-priority]`, `[fig-roadmap]`. For each initiative: owner, duration, what done looks like.

## How we got here
`[fig-trace]`, `[fig-coverage]`. Two sentences on method.

## Appendix
`[fig-unthemed]` — observations that did not form a pattern; any severity-3 item here should already have been promoted above. Then finding, theme, recommendation, initiative tables (from the tables dir).
