# Done for engagement-1
- All three seed sources routed with intent; zero unrouted at close.
- Coverage over the taxonomy: no node outstanding; the reporting-line
  conflict visible as contested BEFORE the response round, resolved after.
- One full ask round: asks curated (artifact-shaped where possible),
  accepted + sent through gates (in the session record), responses put
  back via respond, settlement DERIVED — never declared. The non-answer
  leaves its ask unsettled or closed-with-reason; the silence leaves an
  open debt visible in state().
- The exam (questions.md): every answer's standing matches expectation,
  produced by answers.ground — no hand-assigned standings.
- check.run: zero errors at every checkpoint.
- render: information-request compiles, views build; the docx emit is a
  NAMED refusal (py seam is Phase 2) — recorded, not worked around.
- Session record: every verb, both gates, budget with estimates vs
  actuals. Zero hand-edits to any verb-owned store.
- A synthesis source registered (the consolidated approval model), with
  grounds, cited by capture, standings chaining correctly.
