# M41 WP-O4 prose self-review (orchestrator-written)

Scope: agents/consult-surveyor.md, agents/consult-librarian.md,
skills/consult-orchestrate/SKILL.md. Written by the orchestrator directly
(bounded prose over contracts it authored); reviewed against the spec's
two policed risks.

## What changed

- **Surveyor**: (1) `objective` admitted as a dispatch key — dispatch
  data, not a file read, so the closed reading contract is untouched in
  its file list; the key's own entry states the license boundary
  ("narrows your attention, never your honesty"). (2) Initial mode gains
  the seeded-skeleton paragraph — refine, never worship; removal is
  proposed, the human executes; a seeded node's existence is not
  evidence. (3) The sufficiency table gains the objective lens ("enough
  FOR the target deliverables", naming which deliverable need makes a
  node thin). (4) Information requests may cite the deliverable in
  client language; pipeline vocabulary stays banned.
- **Librarian**: trigger 6 — a live node outside the objective's scope
  is a structural proposal (removal or scope amendment, named), evidence
  = the brief's objective section. Propose-never-execute restated.
- **SKILL.md**: the central taxonomy dispatch's "two things ride the
  prompt" becomes three — `brief.py <area> --objective` output pasted
  under an `objective:` heading, with the paste-the-none-line rule
  (absence-by-choice is information). The confirm-gate row gains
  `--promote-taxonomy` on the same explicit go-ahead, collision refusal
  named as the human's edit.

## Against the policed risks

- **Skeleton worship**: guarded in the surveyor (initial-mode paragraph)
  and structurally by seed_taxonomy itself (never overwrites staged or
  live). The reference taxonomy's advisory clause is untouched.
- **Objective creep**: the objective reaches only dispatch prompts and
  the placement brief. No render-path or plan-path prose mentions it;
  the advisor's decision payload is untouched (verified: the M37
  dispatch-hint pins pass unmodified).
