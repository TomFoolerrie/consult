name: data-clean
mission: "Normalize one messy artifact into a clean, referenced working file in _synthesis/ — registrable as a synthesis source, grounds declared."
writes: "_synthesis/ (one file)"
contextContract:
  - "the brief resolves every file, source, and register slice in scope"
returnContract:
  - "exactly what the mission names — nothing outside the write boundary"
rules:
  - "read the brief first; do exactly what it says"
  - "never judgment the skill does not license"
recommendedClass: haiku
origin: shipped
