"""CONSULT v2 — the engagement brain.

One package, sixteen modules. The laws every module obeys are in DESIGN.md;
each module's docstring is its contract: what it owns, what it writes, what
it refuses. In the mock-out stage every public function raises
NotImplementedError; the signature and docstring ARE the design.
"""

__version__ = "2.0.0-alpha"
