# Engagement register backups (auto-created by improvement_log.py)
engagements/**/register.backup-*.json
engagements/**/*.backup-*.json

# Build artifacts
*.zip
__pycache__/
*.pyc

# Generated workbooks
*.xlsx

# T20 Slice-1 e2e scratch engagement (test removes it on exit; ignored as a
# safety net in case a run is interrupted before cleanup).
engagements/__t20__/

# Rendered Word deliverables (regenerable from MD via render_deliverables.py)
*.docx
