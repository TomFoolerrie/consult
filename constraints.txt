# constraints.txt — the known-good resolution (M64 Part B).
# CI installs with `pip install -r requirements.txt -c constraints.txt`, so a
# new latest-on-PyPI release cannot silently change what the suite runs on.
# Refresh deliberately: bump a pin, run the suite, commit both together.
python-docx==1.2.0
PyYAML==6.0.1
pytest==9.1.1
lxml==6.1.1
typing_extensions==4.16.0
iniconfig==2.3.0
packaging==24.0
pluggy==1.6.0
Pygments==2.20.0
